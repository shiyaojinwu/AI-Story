版本更新
更新时间
更新内容
2025.11.17
更新建议模型选型和部署方案
2025.11.10
完善交互和产品细节
产品描述
定位：一个极简的“故事到视频”生成练手项目，强调功能跑通与架构可扩展。用户新建故事后，系统自动生成分镜描述，并在 Storyboard 中以视觉卡片展示每个镜头，可单独进入详情页进行生成、选择转场效果与配音。
主要功能
- 从故事输入到视频生成的完整链路；
- Storyboard 可视化分镜管理；
- 每个镜头支持单独编辑与生成；
- 支持视频转场与配音功能（视频转场功能可选，根据时间精力可额外扩展）；
- 一键批量生成视频。
交互流程（图为AI生成，部分细节会有错误，按大致规范来即可，同时 PC 主要参考流程，页面细节可自定义）
暂时无法在飞书文档外展示此内容
交互描述
页面
详细交互流程
Create（新建故事页）
- 用户输入故事文本和选择风格（电影 / 动画 / 写实）
- 点击【生成故事】后触发后端接口 
- 系统显示加载状态，等待 LLM 返回分镜 JSON
- 生成成功后自动导航至Storyboard，若生成失败，弹出错误提示并可重试
Storyboard（分镜页）
- 进入页面后加载当前项目的分镜卡片（横向滚动展示）-
- 用户可左右滑动浏览卡片，每张卡片显示缩略图、标题、状态、详情按钮
-  点击【详情】进入分镜详情页] 
- 点击底部【生成视频】按钮，触发 启动视频合成任务
- 合成完成后，状态更新为“完成”，跳转至预览页
Shot Detail（分镜详情页）
- 页面顶部显示返回导航和镜头状态标签
- 用户可查看或编辑 Prompt 文本框- 选择视频过渡效果（Ken Burns / Crossfade / Volume Mix）
-  填写 Narration 文本内容- 点击【Generate Image】调用重新图像生成
- 系统实时更新生成状态（未生成 → 生成中 → 已生成）
- 完成后用户可返回 Storyboard 查看更新结果
Assets（资产库）
- 页面顶部为搜索框，可按故事名称或生成时间筛选
- 列表以卡片网格形式展示故事（含缩略图、标题、时间）- 点击任意卡片后直接跳转进入预览页（极简模式）
Preview（成品预览页）
- 用户可播放、暂停、拖动时间轴、调整音量
- 可点击【Export Video】导出成品文件
工程方案
Android 客户端
模块
技术栈
功能说明
框架
Kotlin + Jetpack Compose
负责整体 UI 架构与导航流；对应 iOS 五个核心页面。
网络层
Retrofit + OkHttp + WebSocket
实现后端通信与进度推送；支持异步任务管理。
状态管理
ViewModel + Flow + Room
统一数据流管理，缓存故事项目、图像与视频信息。
多媒体处理
ExoPlayer + MediaStore
播放视频预览、展示关键帧与语音文件。
文件管理
Scoped Storage + Room
持久化存储生成文件及参数。
运行环境
Android 14+
可通过局域网或本机服务端进行通信，支持离线使用。
iOS 客户端
模块
技术栈
功能说明
框架
SwiftUI + Combine
构建界面与响应式状态流；负责 Create、Storyboard、Shot Detail、Assets、Preview 五个页面。
网络层
URLSession + WebSocket
调用 RESTful API 并监听任务进度；支持断线重连与任务状态同步。

状态管理
ObservableObject + AppStorage
管理项目列表、生成状态、用户偏好（主题、语言）。

多媒体处理
AVKit + Photos
本地播放视频、预览图片、导出生成内容到系统相册。
缓存与文件系统
FileManager + SQLite

缓存生成文件与元数据，支持离线浏览与快速加载。

运行环境
iOS 17+（Apple Silicon）
与本地 Go 服务端通信，默认运行于同一设备或局域网。
 PC 客户端
模块
技术栈
功能说明
框架
QT QML
负责 UI 框架，构建界面与响应式状态流；负责 Create、Storyboard、Shot Detail、Assets、Preview 五个页面。
网络层
Qt Network 模块
调用 RESTful API 并监听任务进度；支持断线重连与任务状态同步。
状态管理
ViewModel 
统一数据流管理，缓存故事项目、图像与视频信息。
多媒体处理
FFmpeg+OpenGL
本地播放视频、预览图片、导出生成内容到文件夹目录。
缓存与文件系统
QFile + SQLite

缓存生成文件与元数据，支持离线浏览与快速加载。

运行环境
Windows/Mac
可通过局域网或本机服务端进行通信，支持离线使用
服务端部署建议
Server部署与模型部署分离，建议Server部署在字节云上，模型部署在本地，通过FRP穿透，由服务端对外提供统一服务

模块
技术栈
功能说明
Server框架
Gin/Django/nginx
提供统一 API 服务入口；支持异步模型调用与任务队列。
python生态可能会稍好些
模型能力调用
HTTP/RPC 与 服务端交互

调用本地模型能力，建议不要与Server耦合，本地独立部署

Agent架构/模型运行监控（如需）

1. LangChain/LangGraph
2. LangSmith

1. Agent架构设计（React/Plan And Execute/Agent as Tool）
2. Agent任务分发与状态跟踪
3. 提供提示词改写之类能力

数据/资产管理
1. PostgreSQL / MySQL
2. 字节云TOS存储
1. 用户信息、产物信息、登录信息和操作信息等
2. 用户资产，生成产物需要上传永久保存
Server部署（Django为例）
1. （如选择字节云部署）创建远端 Server 实例
  1. 登录字节云控制台（https://console.volcengine.com/）。
  2. 选择「云服务器 ECS」。
  3. 创建实例：选择合适的镜像（如 Ubuntu 22.04）、CPU、内存及网络带宽。
  4. 分配公网 IP，配置安全组开放必要端口（如 22、80、443）。
  5. 运行环境：Python 3.10+、Django、Gunicorn、Nginx
  6. 数据库：PostgreSQL / MySQL（按实际情况替换）
2. SSH 登录服务器
ssh root@<你的ECS公网IP>
3. 基础环境安装
apt update && apt upgrade -y
apt install -y git curl build-essential python3 python3-pip python3-venv
4. 拉代码
cd /var/www/my_django_app
git clone <你的项目仓库地址> .
5. python环境
// 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

// 升级 pip 并安装依赖
pip install --upgrade pip
pip install -r requirements.txt

6. 配置/使用 Gunicorn 启动 Django
// 修改 settings.py
ALLOWED_HOSTS = ["你的域名", "你的ECS公网IP"]

//配置静态文件
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / "staticfiles"

//测试运行
source venv/bin/activate
gunicorn your_project_name.wsgi:application --bind 0.0.0.0:8000
7. Nginx 反向代理
apt install -y nginx

// 创建站点配置
vim /etc/nginx/sites-available/my_django_app
/**
server {
    listen 80;
    server_name 你的域名 或 ECS公网IP;

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias /var/www/my_django_app/staticfiles/;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/var/www/my_django_app/gunicorn.sock;
    }
}
**/

//启用站点并重启 Nginx
ln -s /etc/nginx/sites-available/my_django_app /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
systemctl enable nginx
FRP内网穿透（模型部署）
由于模型需要离线部署，GPU推理机器比较贵，可以考虑使用内网穿透的方案，将模型能力接口暴露给字节云Server
FRP 需要两部分：
- frps：部署在公网服务器（比如你有字节云 / 阿里云 / 腾讯云）
- frpc：部署在本地 Windows 电脑
1. 在你的公网服务器上配置 frps（Linux）
  1. 下载 frp（去 GitHub 下载对应版本）
  2. 创建 /etc/frp/frps.ini + 启动服务
[common]
bind_port = 7000
dashboard_port = 7500
dashboard_user = admin
dashboard_pwd = admin123

./frps -c ./frps.ini
2. 在 Windows 本地运行 frpc
  1. 下载 frp（Windows 版）
  2. 在 Windows 创建 frpc.ini + 启动：
// 示例
[common]
server_addr = <你的公网服务器IP>
server_port = 7000

[web]
type = tcp
local_port = 8000
remote_port = 18000

// 启动
frpc.exe -c frpc.ini

模型选型建议（不追求效果，最低资源开销） 
模型类型
功能描述
输入
输出
建议模型和部署方案
LLM（大语言模型）
故事结构化、分镜生成、旁白文案生成
用户输入的故事文本与风格信息
分镜 JSON（包含场景标题、Prompt、旁白、BGM 建议）
Qwen2.5-0.5B
- Ollama部署即可
Text-to-Image（文生图）
根据分镜中的 Prompt 生成关键帧图像
Prompt 文本 + 风格参数
关键帧图像（JPG/PNG）
Stable Diffusion Turbo
- 建议diffusers部署
Image-to-Video（图生视频，可选）
将生成的关键帧拼接为动态镜头片段
单帧图像 + 转场参数
短视频片段（MP4）

Stable-Video-Diffusion-Img2Vid
- 建议diffusers部署
TTS（Text-to-Speech）
将旁白文本转换为语音，用于视频配音
Narration 文本

语音文件（MP3/WAV）
CosyVoice-mini
- 通过fastapi启动cosyvoice部署脚本
分工建议
整个项目的复杂度较高，建议小组内灵活分工，大致可分为：
- 前端能力（客户端UI交互和本地ffmpeg合成逻辑等）
- 后端能力（结构化信息存储、api实现、串联生成服务等）
- 模型能力（不同模态生成模型的部署，输入输出联调、PE优化等）
建议可以按这三个维度进行分工配合，可以把功能拆细，各自闭环一条全技术链路，也可以直接按不同端拆分
FAQ
- 完成形式：按小组形式进行，每个组选出一个组长，组长需要负责小组分工、信息汇总、进度管理等日常事务的推进。文档管理推荐使用飞书文档，每个小组开一个汇总文档，把每个项的文档链接汇总上去，文档形式参考 xxx小组文档汇总
- 代码管理：代码托管推荐使用github，每个小组一个创建一个仓库，官网地址：https://github.com/
- AI Coding：可以使用 AI coding 来提升效率，但要为自己交付的代码质量和结果负责
- 关于考核：
  - 团队打分：功能完成度 60%，体验 20%，文档设计&总结 20%
  - 个人打分：团队贡献度、代码贡献度、答辩分
